{% extends 'base.html.twig' %}

{% block body %}
<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap-theme.min.css" integrity="sha384-rHyoN1iRsVXV4nD0JutlnGaslCJuC7uwjduW9SVrLvRYooPp2bWYgmgJQIXwl/Sp" crossorigin="anonymous">
        <h1>Mes commentaires</h1>
 
            <div class="container row">
                <div class="grid col-md-12">
        {% for commentaire in commentaires %}
            
                    <div class="card" style="width: 15rem;">
                        <div class="card-body">
                            <h5 class="card-title"><a href="#">{{'list.Depart'|trans}}: {{ commentaire.trajet.VilleDepart }}</a></h5>
                            <h5 class="card-title"><a href="#"> {{'list.Destination'|trans}}: {{ commentaire.trajet.VilleArrive }}</a></h5>
                            <p class="card-text">{{'list.le'|trans}} {{ commentaire.trajet.DateDepart|date('d:m:y') }} {{'list.prep'|trans}} {{ commentaire.trajet.HeureDepart|date('h:m:i') }}</p>
                            <p class="card-text">{{'list.place'|trans}} {{ commentaire.trajet.NbrePlace }}</p>
                            <h5 class="card-title text-success" style="font-weight: bold; font-size: 1.5rem;">{{'list.Prix'|trans}}: {{ commentaire.trajet.prix|number_format(0, ',', ' ') }} £</h5>
                            <h4 class="card-text">Commentaire:</h4>
                            <p>{{ commentaire.utilisateur.nom }}: {{ commentaire.contenu }}</p>
                            <a href = "{{ path('commentaire.edit', {id: commentaire.id})}}"><button type="button" class="btn btn-primary">{{('reserver.edit'|trans)}}</button></a>
                            <a href = "{{ path('commentaire.delete', {id: commentaire.id})}}"><button type="button" class="btn btn-danger">{{('reserver.del'|trans)}}</button></a>
                        </div>
                    </div>
                    <div>
                        <p>   </p>
                    </div>
                

        {% endfor %}
                </div>
            </div>
    


{% endblock %}