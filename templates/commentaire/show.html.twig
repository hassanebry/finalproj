{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{'show.title'|trans}} <i>{{ commentaire.trajet.VilleDepart }}</i> {{'list.prep'|trans}} <i>{{ commentaire.trajet.VilleArrive }}</i> {{'list.le'|trans}} <i>{{ commentaire.trajet.DateDepart|date("d/m/Y") }}</i> :</h1>
    <div class="media" style="margin-top: 60px;">
        <div class="media-body">
            <div class="row">
                <div class="col-sm-10">
                    <h3 class="media-heading"><strong>{{ commentaire.trajet.VilleDepart }}</strong> <i>({{ commentaire.trajet.DateDepart|date("d/m/Y") }})</i></h3>
                </div>

                <div class="col-sm-2">
                    <i class="pull-right">{{'show.pub'|trans}} {{ commentaire.trajet.DateAjout|date("d/m/Y") }}</i>
                </div>
            </div>

            <p>
                <strong>{{ commentaire.trajet.VilleDepart }}</strong>
                <small> - <i>{{ commentaire.trajet.VilleArrive }}</i></small>
            </p>

            <p>{{ commentaire.trajet.DateDepart|date("d/m/Y") }}</p>

            <p>
                <strong>{{'show.depart'|trans}} :</strong> {{ commentaire.trajet.HeureDepart|date("h:i:s") }}
            </p>
            <p>
                <strong>{{'show.arrive'|trans}} :</strong> {{ commentaire.trajet.HeureArrive|date("h:i:s") }}
            </p>
			<p>
                <strong>{{'list.Prix'|trans}} :</strong> {{ commentaire.trajet.prix }}
            </p>
			<p>
                <strong>{{'show.Conducteur'|trans}} :</strong> {{ commentaire.utilisateur.nom }} {{ commentaire.utilisateur.prenom }}
            </p>
            {% for comm in commentaire %}
                <p>
                    <strong>commentaire :</strong> {{ commentaire.contenu }}
                </p>
            {% endfor %}
            <p>
                <strong>commentaire :</strong> {{ commentaire.contenu }}
            </p>
            
            <div class="row">
                <div class="col-sm-12 text-right">
                    <a class="btn btn-default" href="/trajet">
                        <span class="glyphicon glyphicon-menu-left" aria-hidden="true"></span>
                        {{'show.list'|trans}}
                    </a>

                    <a class="btn btn-primary" href="#">
                        <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
                        {{'show.edit'|trans}}
                    </a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}